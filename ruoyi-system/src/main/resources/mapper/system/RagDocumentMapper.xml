<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.RagDocumentMapper">

    <resultMap type="com.ruoyi.common.core.domain.entity.RagDocument" id="RagDocumentResult">
        <id property="id" column="id"/>
        <result property="fileName" column="file_name"/>
        <result property="filePath" column="file_path"/>
        <result property="fileSize" column="file_size"/>
        <result property="fileType" column="file_type"/>
        <result property="status" column="status"/>
        <result property="chunkCount" column="chunk_count"/>
        <result property="errorMsg" column="error_msg"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectRagDocumentList" parameterType="com.ruoyi.common.core.domain.entity.RagDocument" resultMap="RagDocumentResult">
        select * from rag_document
        <where>
            <if test="fileName != null and fileName != ''">and file_name like concat('%', #{fileName}, '%')</if>
            <if test="fileType != null and fileType != ''">and file_type = #{fileType}</if>
            <if test="status != null and status != ''">and status = #{status}</if>
        </where>
        order by create_time desc
    </select>

    <select id="selectRagDocumentById" parameterType="Long" resultMap="RagDocumentResult">
        select * from rag_document where id = #{id}
    </select>

    <insert id="insertRagDocument" parameterType="com.ruoyi.common.core.domain.entity.RagDocument" useGeneratedKeys="true" keyProperty="id">
        insert into rag_document
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="fileName != null">file_name,</if>
            <if test="filePath != null">file_path,</if>
            <if test="fileSize != null">file_size,</if>
            <if test="fileType != null">file_type,</if>
            <if test="status != null">status,</if>
            <if test="chunkCount != null">chunk_count,</if>
            <if test="errorMsg != null">error_msg,</if>
            <if test="createBy != null">create_by,</if>
            create_time
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="fileName != null">#{fileName},</if>
            <if test="filePath != null">#{filePath},</if>
            <if test="fileSize != null">#{fileSize},</if>
            <if test="fileType != null">#{fileType},</if>
            <if test="status != null">#{status},</if>
            <if test="chunkCount != null">#{chunkCount},</if>
            <if test="errorMsg != null">#{errorMsg},</if>
            <if test="createBy != null">#{createBy},</if>
            sysdate()
        </trim>
    </insert>

    <update id="updateRagDocument" parameterType="com.ruoyi.common.core.domain.entity.RagDocument">
        update rag_document
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="chunkCount != null">chunk_count = #{chunkCount},</if>
            <if test="errorMsg != null">error_msg = #{errorMsg},</if>
            update_time = sysdate()
        </set>
        where id = #{id}
    </update>

    <delete id="deleteRagDocumentById" parameterType="Long">
        delete from rag_document where id = #{id}
    </delete>

</mapper>
